[project]
name = "provider-charm"
version = "0.0.0"
description = "Test provider charm for integration tests"
requires-python = ">=3.10"
dependencies = [
    "ops",
    "cryptography",
    "charmlibs-interfaces-tls_certificates",
]

[tool.uv.sources]
charmlibs-interfaces-tls_certificates = { path = "library", editable = true }
